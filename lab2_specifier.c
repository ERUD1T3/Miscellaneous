#include <stdio.h>

void read_file(char* file_name); 
# read the file in a variable

void reformat_and_save_file(char*); 
# reformart the variables as each line ask how and save in a new file adding a new column


int main(int argc, char* argv[]) {
  
  return 0;
}

void read_file(char* file_name) {
  FILE* ifp;
  ifp = fopen(file_name, "r");
  while 
}
